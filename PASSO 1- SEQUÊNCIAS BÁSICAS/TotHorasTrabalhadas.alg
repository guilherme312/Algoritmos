algoritmo "semnome"
var
  D, MultH, MultD: Real
inicio
   Escreva("Quntos dias trabalhados? ")
   Leia(D)
   MultH <- D*8
   MultD <- MultH*25
   EscrevaL("Total do salário é: ",MultD)
fimalgoritmo